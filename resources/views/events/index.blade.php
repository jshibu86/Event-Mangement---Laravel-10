@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between mb-4"><h2>All events</h2><a class="btn btn-primary" href="{{route('events.create')}}">Create event</a></div><div class="card p-3"><form class="row g-2 mb-3" method="get"><div class="col-md-6"><div class="input-group"><span class="input-group-text"><i class="bi bi-search"></i></span><input class="form-control" name="search" value="{{$search}}" placeholder="Search by event name, venue, or description"></div></div><div class="col-auto"><button class="btn btn-outline-primary">Search</button>@if($search)<a class="btn btn-link" href="{{route('events.index')}}">Clear</a>@endif</div></form><table class="table mb-0"><thead><tr><th>Event</th><th>Date</th><th>Status</th><th>Budget</th><th></th></tr></thead><tbody>@forelse($events as $event)<tr><td><strong>{{$event->name}}</strong><br><small>{{$event->venue}}</small></td><td>{{$event->event_date->format('d M Y')}}</td><td><span class="badge text-bg-secondary">{{$event->status}}</span></td><td>₹{{number_format($event->grand_total)}}</td><td><a href="{{route('events.show',$event)}}" class="btn btn-sm btn-outline-primary">Open</a><a href="{{route('events.collection',$event)}}" class="btn btn-sm btn-outline-secondary ms-1">Collection</a></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-4">No events match your search.</td></tr>@endforelse</tbody></table></div>
@endsection
