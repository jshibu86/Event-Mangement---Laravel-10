@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h2 class="mb-0">{{ucfirst($type)}}</h2><p class="text-muted mb-0">Reusable building blocks for your next gathering.</p></div><a class="btn btn-primary" href="{{route('masters.create',$type)}}">Add {{rtrim($type,'s')}}</a></div>
<div class="card p-3"><form class="row g-2 mb-3" method="get"><div class="col-md-6"><div class="input-group"><span class="input-group-text"><i class="bi bi-search"></i></span><input class="form-control" name="search" value="{{$search}}" placeholder="Search by name, category, or description"></div></div><div class="col-auto"><button class="btn btn-outline-primary">Search</button>@if($search)<a class="btn btn-link" href="{{route('masters.index',$type)}}">Clear</a>@endif</div></form><table class="table align-middle mb-0"><thead><tr><th>Name</th><th>Category</th><th>Historical rating</th><th>Played</th><th></th></tr></thead><tbody>@forelse($records as $record)<tr><td><strong>{{$record->name}}</strong><br><small class="text-muted">{{Str::limit(strip_tags($record->description),60)}}</small></td><td>{{$record->category ?: '—'}}</td><td>★ {{number_format($record->average_rating,1)}} / 5</td><td>{{$record->times_played}} times</td><td><a class="btn btn-sm btn-outline-primary" href="{{route('masters.edit',[$type,$record])}}">Edit</a></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-4">No {{strtolower($type)}} match your search.</td></tr>@endforelse</tbody></table></div>
@endsection
