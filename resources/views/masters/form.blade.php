@extends('layouts.app') @section('content')<div class="card p-4 col-lg-8 mx-auto"><h2>{{ $record ? 'Edit' : 'Add' }} {{rtrim($type,'s')}}</h2><form method="post" action="{{$record?route('masters.update',[$type,$record]):route('masters.store',$type)}}">@csrf @if($record)@method('PUT')@endif <div class="mb-3"><label>Name</label><input class="form-control" name="name" value="{{old('name',$record?->name)}}" required></div><div class="row"><div class="col mb-3"><label>Category</label><input class="form-control" name="category" value="{{old('category',$record?->category)}}"></div>@if($type==='games')<div class="col mb-3"><label>Min players</label><input type="number" class="form-control" name="min_players" value="{{$record?->min_players}}"></div><div class="col mb-3"><label>Max players</label><input type="number" class="form-control" name="max_players" value="{{$record?->max_players}}"></div>@endif</div><div class="mb-3"><label>How it works / description</label><textarea class="form-control" rows="5" name="description">{{old('description',$record?->description)}}</textarea></div><div class="mb-3"><label>Video links <small class="text-muted">one per line</small></label><textarea class="form-control" name="video_urls">{{old('video_urls',implode("\n",$record?->video_urls ?? []))}}</textarea></div><div class="form-check mb-3"><input class="form-check-input" type="checkbox" name="status" value="1" @checked(old('status',$record?->status ?? true))><label class="form-check-label">Active and selectable</label></div><button class="btn btn-primary">Save</button></form></div>@endsection
