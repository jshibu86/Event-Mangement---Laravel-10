@extends('layouts.app') @section('content')<div class="hero p-5 mb-4"><div class="row align-items-center"><div class="col"><p class="text-uppercase small mb-2">Plan with intention</p><h1 class="display-6 fw-bold">Make every gathering memorable.</h1><p class="mb-0 opacity-75">Build timelines, watch every rupee, and learn what your guests loved.</p></div><div class="col-auto"><a class="btn btn-light btn-lg" href="{{route('events.create')}}"><i class="bi bi-plus-lg"></i> New event</a></div></div></div><div class="d-flex justify-content-between align-items-center mb-3"><h3 class="mb-0">Upcoming celebrations</h3><a href="{{route('events.index')}}">View all</a></div><div class="row g-3">@forelse($events as $event)<div class="col-md-6 col-xl-4"><div class="card h-100 p-3"><span class="badge text-bg-{{ $event->status==='verified'?'success':'warning' }} align-self-start">{{ucfirst($event->status)}}</span><h4 class="mt-3">{{$event->name}}</h4><p class="text-muted mb-1"><i class="bi bi-calendar3"></i> {{$event->event_date->format('D, d M Y')}}</p><p class="text-muted"><i class="bi bi-geo-alt"></i> {{$event->venue ?: 'Venue to be announced'}}</p><div class="border-top pt-3 d-flex justify-content-between"><span>{{$event->agenda_items_count}} agenda items</span><strong>₹{{number_format($event->grand_total)}}</strong></div><a class="stretched-link" href="{{route('events.show',$event)}}"></a></div></div>@empty<div class="col"><div class="card p-5 text-center">Your event calendar is waiting. <a href="{{route('events.create')}}">Create the first event.</a></div></div>@endforelse</div>@endsection
